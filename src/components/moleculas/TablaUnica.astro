---
interface Props {
  align?: "left" | "center" | "right";
}
const { align = "left" } = Astro.props;
const alignClass = align === "right" ? "text-right" : align === "center" ? "text-center" : "text-left";
---
<td class:list={["px-4 py-3 text-[15px]", alignClass]}>
  <slot />
</td>
